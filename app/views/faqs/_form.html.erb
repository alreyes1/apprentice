<%= form_for(@faq) do |f| %>
  <% if @faq.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@faq.errors.count, "error") %> prohibited this faq from being saved:</h2>

      <ul>
      <% @faq.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :questions %><br>
    <%= f.text_field :questions, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label :answers %><br>
    <%= f.text_area :answers, class: "form-control" %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
